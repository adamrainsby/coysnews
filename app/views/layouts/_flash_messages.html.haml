- flash.each do |key, value|
  %div{:class => "alert alert-#{key} flash-messages"}
    %a.close{"data-dismiss" => "alert", :href => "#"} ×
    %ul
      %li
        = value

:javascript
  setTimeout(function () {
    $('.flash-messages').fadeOut();
  }, 2000);
