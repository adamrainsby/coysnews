.col-md-offset-3.col-md-6
  = simple_form_for(@prediction, url: match_prediction_path(@prediction.match, @prediction), method: 'patch', html: { class: 'form-horizontal' }) do |f|
    = render partial: 'form_fields', locals: {f: f}
    = f.button :submit
