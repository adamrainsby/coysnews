%a.list-group-item.text-center{href: '#', class: match.next_match? ? 'active' : ''}
  %h4.list-group-item-heading
    = "#{match.home_team.name} V #{match.away_team.name}"
  %p.list-group-item-text
    Some shit about the game
